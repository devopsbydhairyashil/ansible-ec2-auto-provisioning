#!/usr/bin/env python3
# Placeholder dynamic inventory script that would call AWS APIs (boto3) to gather EC2 instances.
# For security reasons this is a template: install boto3 and configure AWS credentials to use in production.
print('[all]')
print('localhost ansible_connection=local')
